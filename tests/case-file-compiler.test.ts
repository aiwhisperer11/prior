import assert from "node:assert/strict";
import test from "node:test";
import { compileCaseFile, type SourceEvidence } from "../lib/server/case-file-compiler";
const source = (variable: string, kind: SourceEvidence["kind"], value = "confirmed"): SourceEvidence => ({ source_id: `S-${variable}`, source_type: "log", locator: variable, publisher: "system", fragment: value, event_timestamp: "2026-01-01T00:00:00Z", recorded_at: "2026-01-01T00:00:01Z", retrieval_time: "2026-01-01T00:00:02Z", content_hash: `hash-${variable}`, observation_capability: variable, entity: "unit", variable, observed_value: value, units: null, quality: "raw", limitations: variable === "effect" ? "telemetry unavailable" : "", kind });
test("compiler preserves facts, claims, gaps and unmatched evidence without causal promotion", () => { const file = compileCaseFile([source("detection", "observed"), source("signal", "observed"), source("execution", "declared"), source("effect", "observed"), source("unrelated", "observed")]); assert.equal(file.facts.length, 4); assert.equal(file.claims.length, 1); assert.ok(file.telemetry_gaps.includes("effect")); assert.deepEqual(file.template.unmatched_evidence, ["S-unrelated"]); assert.match(file.template.selection_basis, /not causal discovery/); });
