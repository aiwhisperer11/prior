import type { EvidenceScoutResult } from "@/types/evidence-scout";
export default function EvidenceScoutPanel({ scout }: { scout: EvidenceScoutResult }) { return <section className="rounded-lg border border-violet-300 bg-violet-50 p-5 text-sm"><h2 className="text-xl font-semibold">Evidence Scout · retrieved evidence</h2><p className="mt-1">Human evidence: {scout.human_evidence.length} · Inferences: {scout.inferences.length} · Missing/contradictory: {scout.missing_or_contradictory.length}. Retrieval is not causal proof.</p><ul className="mt-3 space-y-3">{scout.retrieved_evidence.map((item, index) => <li key={index} className="rounded border border-violet-200 p-3"><strong>{item.metric}</strong>: {String(item.value)} {item.unit}<br/><span>{item.publisher} · observed {item.observed_at} · {item.verification_status}</span><br/><a className="underline" href={item.source_url} target="_blank" rel="noreferrer">Official source</a><br/><span>Supports: {item.supports.join(", ") || "none"}; weakens: {item.weakens.join(", ") || "none"}</span></li>)}</ul></section>; }
